# Gemini key for Boot.dev agent steps
GEMINI_API_KEY=

# local = free mode, openai = uses your API key
MODE=local

# only needed if MODE=openai
OPENAI_API_KEY=
MODEL=gpt-5

# path to your csv file
DATA_PATH=data/sample_data.csv
